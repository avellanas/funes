<%= form_for(@current_issue) do |f| %>
  <% if @current_issue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@current_issue.errors.count, "error") %> prohibited this current_issue from being saved:</h2>

      <ul>
      <% @current_issue.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %><br>
    <%= f.number_field :number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
